<Page
    x:Class="AppNote.Pages.form"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppNote.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <TextBox AcceptsReturn="True" x:Name="Note" TextWrapping="Wrap"
                     MaxHeight="172"
                     Width="500"
                     Margin="0,20"
                     Height="450"
                     Header="Note content"
                     PlaceholderText="Enter content"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Click="Button_Click" Margin="4">Save</Button>
                <Button Click="Button_Click_1" Margin="4">Note list</Button>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</Page>
